/* eslint-disable */
export type Methods = {
  get: {
  }

  patch: {
  }

  delete: {
  }
}

export default {
  get: () => ({ status: 200 }),
  patch: () => ({ status: 200 }),
  delete: () => ({ status: 200 })
}
