{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:aspida": "npm run build:aspida && cd packages/aspida && node bin/index.js --build",
    "dev:swagger": "npm run build:swagger && cd packages/aspida-swagger && node bin/index.js --build",
    "dev:mock": "npm run build:mock && cd packages/axios-mock-server && node bin/index.js --build",
    "build:aspida": "cd packages/aspida && rimraf dist && tsc --project tsconfig.build.json",
    "build:axios": "cd packages/aspida-axios && rimraf dist && tsc --project tsconfig.build.json",
    "build:fetch": "cd packages/aspida-fetch && rimraf dist && tsc --project tsconfig.build.json",
    "build:ky": "cd packages/aspida-ky && rimraf dist && tsc --project tsconfig.build.json",
    "build:swagger": "cd packages/aspida-swagger && rimraf dist && tsc --project tsconfig.build.json",
    "build:mock": "cd packages/axios-mock-server && rimraf dist && tsc --project tsconfig.build.json",
    "release": "standard-version --skip.tag",
    "lint": "eslint --ext .js,.ts --ignore-path .gitignore .",
    "lint:fix": "npm run lint -- --fix",
    "test": "jest",
    "test:watch": "npm test -- --watch",
    "typecheck": "npm run build:aspida && npm run build:mock && tsc --noEmit"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.3.4",
    "@commitlint/config-conventional": "^8.3.4",
    "@types/jest": "^24.0.25",
    "@types/minimist": "^1.2.0",
    "@types/rimraf": "^2.0.3",
    "@typescript-eslint/eslint-plugin": "2.15.0",
    "@typescript-eslint/parser": "^2.15.0",
    "axios": "^0.19.1",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.9.0",
    "eslint-config-standard": "^14.1.0",
    "eslint-plugin-import": "^2.19.1",
    "eslint-plugin-jest": "^23.4.0",
    "eslint-plugin-node": "^11.0.0",
    "eslint-plugin-prettier": "^3.1.2",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.1",
    "husky": "^4.0.6",
    "jest": "^24.9.0",
    "ky": "^0.16.1",
    "ky-universal": "^0.3.0",
    "lint-staged": "^9.5.0",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.0",
    "standard-version": "^7.0.1",
    "ts-jest": "^24.3.0",
    "ts-node": "^8.6.1",
    "typescript": "^3.7.4"
  }
}
