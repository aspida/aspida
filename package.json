{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:aspida": "npm run build:aspida && cd packages/aspida && node bin/index.js --build",
    "dev:openapi": "npm run build:openapi && cd packages/openapi2aspida && rimraf \"samples/*/*\" && node bin/index.js --build",
    "dev:mock": "npm run build:mock && cd packages/aspida-mock && aspida -b && node bin/index.js --build",
    "dev:path": "npm run build:path && cd packages/pathpida && node bin/index.js --build",
    "build:aspida": "cd packages/aspida && rimraf dist && tsc --project tsconfig.build.json",
    "build:axios": "cd packages/aspida-axios && rimraf dist && tsc --project tsconfig.build.json",
    "build:fetch": "cd packages/aspida-fetch && rimraf dist && tsc --project tsconfig.build.json",
    "build:node-fetch": "cd packages/aspida-node-fetch && rimraf dist && tsc --project tsconfig.build.json",
    "build:ky": "cd packages/aspida-ky && rimraf dist && tsc --project tsconfig.build.json",
    "build:openapi": "cd packages/openapi2aspida && rimraf dist && tsc --project tsconfig.build.json",
    "build:mock": "cd packages/aspida-mock && rimraf dist && tsc --project tsconfig.build.json",
    "build:path": "cd packages/pathpida && rimraf dist && tsc --project tsconfig.build.json",
    "release": "standard-version --skip.tag",
    "release:major": "npm run release -- --release-as major",
    "release:minor": "npm run release -- --release-as minor",
    "release:patch": "npm run release -- --release-as patch",
    "lint": "eslint --ext .js,.ts --ignore-path .gitignore .",
    "lint:fix": "npm run lint -- --fix",
    "test": "jest",
    "test:watch": "npm test -- --watch",
    "typecheck": "npm run build:aspida && npm run build:mock && npm run build:axios && tsc --noEmit"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "@types/jest": "^25.2.1",
    "@types/minimist": "^1.2.0",
    "@types/node-fetch": "^2.5.7",
    "@types/rimraf": "^3.0.0",
    "@typescript-eslint/eslint-plugin": "2.30.0",
    "@typescript-eslint/parser": "^2.30.0",
    "axios": "^0.19.2",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-config-standard": "^14.1.1",
    "eslint-plugin-import": "^2.20.2",
    "eslint-plugin-jest": "^23.8.2",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^3.1.3",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.1",
    "husky": "^4.2.5",
    "jest": "^25.4.0",
    "ky": "^0.19.1",
    "lint-staged": "^10.2.0",
    "node-fetch": "^2.6.0",
    "prettier": "2.0.5",
    "rimraf": "^3.0.2",
    "standard-version": "^7.1.0",
    "ts-jest": "^25.4.0",
    "typescript": "^3.8.3"
  }
}
