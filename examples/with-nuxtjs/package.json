{
  "name": "with-nuxtjs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm-run-all mock:build --parallel nuxt mock:watch",
    "nuxt": "nuxt",
    "mock:build": "axios-mock-server --build",
    "mock:watch": "axios-mock-server --watch"
  },
  "dependencies": {
    "nuxt": "^2.9.2",
    "@nuxtjs/axios": "^5.6.0"
  },
  "devDependencies": {
    "axios-mock-server": "latest",
    "npm-run-all": "^4.1.5"
  }
}
